# Web framework
fastapi>=0.104.0
uvicorn>=0.24.0

# Document processing
pypdf>=3.0.0
python-docx>=0.8.11

# CPU-only torch — pin to latest available CPU build for Python 3.14
# Prevents pip pulling in 2GB of CUDA/nvidia packages
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu

# Embeddings & vector search
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
numpy>=1.24.0

# LangChain (huggingface integration removed — not needed at runtime)
langchain>=0.1.0
langchain-community>=0.0.10

# LLM clients
openai>=1.0.0
groq
tiktoken>=0.5.0

# Config
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0